# 套件驗收規範

本套件同時做單元測試與最外層驗收／整合測試。驗收需涵蓋公開介面、正常與失敗路徑、權限／安全邊界，以及適用的併發、持久化與效能行為；不能只以單元測試替代套件整合驗收。

主產品 kamigo_bot 的 request-only、只允許 mock 外部 HTTP 規則不代表套件省略單元測試。套件單元測試可使用必要隔離替身，但需由真實整合測試補足契約驗證，不把替身測試稱為真實平台驗收。

## 測試檔案與資料隔離

- 按功能／驗收情境拆成獨立測試檔，避免巨型測試檔；密切相關的案例可留在同一檔。
- 每個測試自行建立及維護需要的最小資料集，不依賴其他測試先執行或留下的資料。
- 預設不共用全域 fixtures 或業務情境 seed。setup 僅建立該情境必要資料，不批量建立其他案例用不到的資料。
- 如確有必要，共用 seed 限少數穩定基礎資料集（例如固定目錄／代碼），需明確列出用途；各測試不得修改共用狀態或依賴隱藏資料。
- 測試程式碼優先直接複製，讓各情境自行保有資料建立、請求與斷言；不因重複就抽 helper、shared examples、共用 setup 或測試基底。
- 只有重複維護確實過於繁瑣時，才抽出最小共用程式碼，並在旁註明必要性；不得隱藏情境值、共享可變記錄或引入測試順序依賴。
- Rails／測試框架必要的啟動、測試資料庫隔離與 HTTP mock 工具載入可保留共用；業務情境邏輯預設不共用。
- 使用交易回復或對應清理方式確保資料隔離；隨機執行順序及平行執行不得互相污染。
- 不為拆檔而拆成每個斷言一檔；以情境清楚、資料自主、失敗可定位為準。

## 開發順序：實作 → agent 手動驗收 → 回歸測試

使用者指定先開發功能，再由 agent 使用 computer use／瀏覽器等方式操作實際介面手動驗收，最後才撰寫自動測試，防止後續開發改壞已驗收的功能。不是測試先行，也不是等待使用者代為操作。

- 手動驗收需走真實使用流程，記錄操作情境、觀察結果與未通過項；不得把 request 測試、單元測試通過或讀程式碼稱為手動驗收。
- 發現問題先修正並重新操作確認，再依驗收過的外部行為補回歸測試。既有回歸測試仍可在開發中執行。
- 無 UI 的 API／套件功能，透過實際 API 請求或可執行範例人工檢查公開介面；有產品 UI 的流程仍需透過 UI 操作，不以腳本取代。
- 如環境、帳號或平台能力不足，記錄具體阻礙與未驗收範圍，不宣稱手動驗收完成。
- 主專案後補的測試仍只從 request 最外層驗收、最多 mock 外部 HTTP，保持架構獨立；框架仍補單元與驗收測試。
- 本次新服務的手動驗收由 agent 執行，包含適用的遊戲介面流程；取代先前要求等待使用者親自驗收的開發流程。這不授權操作正式環境資產、付款或任意向真實群組發訊息。

## 手動驗收與自動測試須一致

自動測試必須重現agent實際手動驗收的同一流程：相同前置資料、角色、操作順序、輸入與預期可觀察結果。不得手動驗收A流程卻寫B流程，再宣稱兩者互相證明。

- 每個回歸案例標明對應的手動情境ID及紀錄，包含資料與操作步驟；測試新增帳號的唯一識別可不同，但角色關係與業務資料語意必須相同。
- 主專案仍只做request測試：將實際UI動作對應到其真實HTTP請求，登入與資料建立也走同樣公開流程，不繞過中間步驟、不直接seed業務狀態。
- 瀏覽器行為若無法由request層驗證（例如JavaScript、畫面是否更新、排版），明確保留為手動驗收項，不聲稱request測試有覆蓋。
- 新增負例、邊界或安全案例時，先實際操作／呼叫公開入口驗收，再補該流程的自動測試。不能因測試通過而追認手動驗收。
- 既有測試若缺少相同流程的手動證據，保留回歸保護並標記待補手動驗收；補齊對照前不能列為已完成的雙重驗收。
- 框架單元測試保留既有完整測試要求；其驗收／整合案例同樣應對應實際公開介面驗收流程。
